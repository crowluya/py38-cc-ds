[package]
name = "sort_bench"
version = "0.1.0"
edition = "2021"
description = "A benchmarking tool for comparing sorting algorithms"
authors = ["Sort Bench Tool"]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
rand = "0.8"

[dev-dependencies]
# For testing
